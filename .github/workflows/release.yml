name: Release with Maven

#on:
#  workflow_dispatch:
on:
  push:
    branches:
      - 'release'
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'adopt'
          cache: 'maven'
          server-id: github
      - name: Git Config
        run: |
          git config user.email "actions@github.com"
          git config user.name "GitHub Actions"
      - name: Remove snapshot
        run: versions:set -DremoveSnapshot=true -DgenerateBackupPoms=false
#      - name: Release with Maven
#        run: mvn --batch-mode deploy
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Git tag
        run: |
          CURRENT_PROJECT_VERSION=$(mvn -q -Dexec.executable=echo -Dexec.args='${project.version}' --non-recursive exec:exec)
          git commit -a -m "[skip ci] release $CURRENT_PROJECT_VERSION"
          git tag "release-$CURRENT_PROJECT_VERSION"
          git push origin "release-$CURRENT_PROJECT_VERSION"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
